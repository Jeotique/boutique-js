<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion</title>
    <link rel="stylesheet" href="/assets/css/login.css">
</head>

<%- include("../partials/header.ejs", {hoster, user, path}) %>

<body>
    <% if (message) { %>
        <div id="banner" class="banner <%= message.type %>">
            <p><%= message.text %></p>
        </div>
    <% } %>
    <main>
        <div class="container">
            <div class="form-container sign-in-container">
                <form action="<%= hoster %>/login" method="POST">
                    <h1>Connexion</h1>
                    <input type="email" placeholder="Email" name="email" required/>
                    <input type="password" placeholder="Mot de passe" name="password" required/>
                    <button type="submit">Connexion</button>
                    <p>Pas encore de compte ? <a href="<%= hoster %>/register">S'inscrire</a></p>
                </form>
            </div>
            <div class="overlay-container">
                <div class="overlay">
                    <div class="overlay-panel overlay-left">
                        <h1>Bienvenue de retour !</h1>
                        <p>Pour rester connecté avec nous, veuillez vous connecter avec vos informations personnelles</p>
                        <button class="ghost" id="signIn">Connexion</button>
                    </div>
                    <div class="overlay-panel overlay-right">
                        <h1>Bonjour, Amis !</h1>
                        <p>Entrez vos informations personnelles et commencez votre voyage avec nous</p>
                        <a href="<%= hoster %>/register"><button class="ghost" id="signUp">S'inscrire</button></a>
                    </div>
                </div>
            </div>
        </div>
    </main>
    
    <%- include("../partials/footer.ejs") %>

    <script src="/assets/js/scripts.js"></script>
</body>
</html>
